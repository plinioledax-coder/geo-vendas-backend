# requirements.txt
fastapi
uvicorn[standard]
SQLAlchemy
pandas
openpyxl
tqdm
python-multipart
python-dateutil
geopy

# --- Segurança e Autenticação (Novos) ---
python-jose[cryptography]
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Variáveis de Ambiente (Novo) ---
python-dotenv